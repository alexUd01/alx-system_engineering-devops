#!/usr/bin/env bash
# A bash sctipt that configures a new Ubuntu machine to respect
# certain requrements

apt-get update
apt-get install -y nginx
ufw allow 'Nginx HTTP'
chmod 777 -R /var/www/html/
echo 'Hello World' > /var/www/html/index.html
service nginx start
